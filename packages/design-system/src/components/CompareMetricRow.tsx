import { Text, View, StyleSheet } from 'react-native';
import { colors, radius, spacing, typography } from '../tokens/index.js';
export function CompareMetricRow({label,leftLabel,leftValue,rightLabel,rightValue}:{label:string;leftLabel:string;leftValue:number;rightLabel:string;rightValue:number}){const clamp=(v:number)=>Math.max(0,Math.min(5,v));return <View style={styles.root}><Text style={styles.label}>{label}</Text><View style={styles.side}><Text style={styles.value}>{leftLabel} {leftValue.toFixed(1)}</Text><View style={styles.track}><View style={[styles.fill,{width:`${clamp(leftValue)/5*100}%`}]}/></View></View><View style={styles.side}><Text style={styles.value}>{rightLabel} {rightValue.toFixed(1)}</Text><View style={styles.track}><View style={[styles.fill,{width:`${clamp(rightValue)/5*100}%`}]}/></View></View></View>}
const styles=StyleSheet.create({root:{gap:spacing[2]},label:{fontFamily:typography.family.sans,fontSize:typography.size.sm,fontWeight:typography.weight.bold,color:colors.ink[950]},side:{gap:spacing[1]},value:{fontFamily:typography.family.sans,fontSize:typography.size.sm,color:colors.ink[800]},track:{height:6,borderRadius:radius.pill,backgroundColor:colors.line[200],overflow:'hidden'},fill:{height:6,borderRadius:radius.pill,backgroundColor:colors.action[500]}});
