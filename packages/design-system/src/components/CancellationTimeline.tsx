import { Text, View, StyleSheet } from 'react-native';
import { colors, spacing, typography } from '../tokens/index.js';
const rows=[['12시간 초과 전','전액 반환 기본안'],['3–12시간','부분 반환 기본안'],['3시간 미만 / No-show','강한 패널티 기본안']] as const;
export function CancellationTimeline(){return <View style={styles.root}><Text style={styles.heading}>취소 기준</Text>{rows.map(([time,copy])=><View key={time} style={styles.row}><Text style={styles.time}>{time}</Text><Text style={styles.copy}>{copy}</Text></View>)}<Text style={styles.note}>실제 반환액과 마감시간은 서버 계산 결과가 우선해요.</Text></View>}
const styles=StyleSheet.create({root:{gap:spacing[2]},heading:{fontFamily:typography.family.sans,fontSize:typography.size.md,fontWeight:typography.weight.bold,color:colors.ink[950]},row:{flexDirection:'row',justifyContent:'space-between',gap:spacing[3],paddingVertical:spacing[2],borderBottomWidth:1,borderBottomColor:colors.line[200]},time:{fontFamily:typography.family.sans,fontSize:typography.size.sm,fontWeight:typography.weight.semibold,color:colors.ink[800]},copy:{flex:1,textAlign:'right',fontFamily:typography.family.sans,fontSize:typography.size.sm,color:colors.ink[600]},note:{fontFamily:typography.family.sans,fontSize:typography.size.xs,color:colors.ink[600]}});
