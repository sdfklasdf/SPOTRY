import { Text, View, StyleSheet } from 'react-native';
import { colors, radius, spacing, typography } from '../tokens/index.js';
export interface TrustItem { label:string; value:string; verified:boolean; }
export function CoachTrustBlock({items}:{items:readonly TrustItem[]}){return <View style={styles.card}><Text style={styles.heading}>코치 신뢰 정보</Text>{items.map((x,i)=><View key={`${i}-${x.label}`} style={styles.row}><View><Text style={styles.label}>{x.label}</Text><Text style={styles.value}>{x.value}</Text></View><Text style={x.verified?styles.verified:styles.claim}>{x.verified?'검증 완료':'본인 입력'}</Text></View>)}</View>}
const styles=StyleSheet.create({card:{gap:spacing[3],padding:spacing[4],borderWidth:1,borderColor:colors.line[200],borderRadius:radius.lg},heading:{fontFamily:typography.family.sans,fontSize:typography.size.lg,fontWeight:typography.weight.bold,color:colors.ink[950]},row:{minHeight:44,flexDirection:'row',alignItems:'center',justifyContent:'space-between',gap:spacing[3]},label:{fontFamily:typography.family.sans,fontSize:typography.size.xs,color:colors.ink[600]},value:{fontFamily:typography.family.sans,fontSize:typography.size.sm,fontWeight:typography.weight.semibold,color:colors.ink[950]},verified:{fontFamily:typography.family.sans,fontSize:typography.size.xs,fontWeight:typography.weight.bold,color:colors.semantic.successText},claim:{fontFamily:typography.family.sans,fontSize:typography.size.xs,color:colors.ink[400]}});
